import { describe, it } from "vitest"

describe("Header", () => {
  it("should display at least one nav item", async () => {
    // render(<Header />)
    // await screen.findByText("Services")
    // const itemsNav = screen.getAllByRole("link")
    // expect(itemsNav.length).toBe(4)
  })
})
